# Railway(nixpacks) 빌드 설정
# opencv-python(일반판)은 libGL 이 필요하다 — headless 를 못 쓰는 이유는
# simple-lama-inpainting 이 일반판을 의존해 이중설치가 되기 때문(requirements.txt 참조).
[phases.setup]
aptPkgs = ["libgl1", "libglib2.0-0"]
